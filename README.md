# UOK Assignment 2 — Linux VM (SaaS / Cloud)

**Student:** Ishimwe Jean Francois  
**Project path:** `C:\Users\IT FAir\Desktop\uokclass2`  
**Live site:** `http://YOUR_PUBLIC_IP/` (update after deploy)

## Repository contents

| File | Purpose |
|------|---------|
| `index.html` | Main homepage (your name) |
| `assign2.html` | Same as class demo filename |
| `ASSIGNMENT2.md` | Five requirements + screenshot checklist |
| `DEPLOY_GITBASH.txt` | Copy-paste commands for Git Bash |

## Quick deploy (class method)

1. Push this repo to GitHub (lecturer link).
2. From Git Bash: `scp` HTML to VM `/tmp/`, then `sudo cp` to `/var/www/html/`.
3. Open `http://PUBLIC_IP/` in browser.

See **DEPLOY_GITBASH.txt** for exact commands.
